# Codex UI metadata for the korea100studio skill.
# Entry point for behavior/instructions is SKILL.md — this file only feeds
# Codex's skill picker/interface surface (mirrors fireworks-tech-graph's shape).
name: korea100studio
entrypoint: SKILL.md
interface:
  display_name: "korea100studio"
  short_description: "Turn a process description into a swimlane process board, audit it, and animate it"
  default_prompt: "Use $korea100studio to turn the user's process or workflow description into a board-v1 JSON, render it as a vertical swimlane SVG (optional PNG) with scripts/board.mjs render, audit composition quality with the audit subcommand, and render a stage-ordered reveal animation with the motion subcommand."
